<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog - Trishastik Bharat Sustainable Farms</title>
    <link href="/stylesheet/output.css" rel="stylesheet">
    <link rel="icon" href="/images/logo.png" type="image/png">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-slate-600">
    <%- include("./includes/navbar.ejs") %>
    <div class="relative z-10 flex flex-col items-center justify-center h-full text-center text-white"><h1 class="text-4xl md:text-6xl font-bold"><%- include("./includes/flash.ejs") %></h1></div>
    
    <section class="py-16 bg-white">
        <div class="container mx-auto text-center">
            <% if(currUser.email === "freeforfire15@gmail.com") { %>
            <form class="mt-6 px-8 py-3 bg-green-600 hover:bg-green-700 rounded-lg text-white text-lg" method="GET" action="/blognew">
                <button>Create New Listing</button>
              </form>
              <% } %>
            <h2 class="text-3xl font-bold mb-8 text-gray-800">Our Blog</h2>
            <p class="text-lg text-gray-600 mb-8">Explore insightful articles about sustainable farming, eco-friendly practices, and more.</p>
            
            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8">
                 <% for (let listing of allListings) { %>
                <div class="bg-white p-6 rounded-lg shadow-lg">
                    <img src="<%= listing.image %>" alt="The Future of Organic Farming" class="w-full h-48 object-cover rounded-md mb-4">
                    <h3 class="text-xl font-semibold text-gray-800"><%= listing.title %></h3>
                    <p class="text-gray-600 mt-2"><%= listing.description %></p>
                    <p style="margin-top: 1rem;" class="text-gray-400 text-sm"><%= listing.publishedon %></p>
                 
                </div>   <% } %>

                
               
            </div>
        </div>
    </section>

    <%- include("./includes/footer.ejs") %>
    
</body>
</html>
