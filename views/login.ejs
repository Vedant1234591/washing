<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Wanderlust</title>
  
  <!-- External Stylesheets -->
  <link rel="stylesheet" href="/stylesheet/style.css" />
  <link rel="stylesheet" href="/stylesheet/output.css" />
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous" />
  
  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Barlow:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@300;400;500;600&display=swap" rel="stylesheet" />
  
  <style>
    /* Background and Global Styles */
    body {
      font-family: 'Barlow', sans-serif;
      background-image: url('/images/veg.png'); /* Agricultural Background */
      background-size: cover;
      background-position: center;
      color: #fff;
      height: 100vh;
      margin: 0;
    }

    /* Container Styling */
    .container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100%;
      flex-direction: row; /* Align items horizontally */
      gap: 30px; /* Space between the two boxes */
    }

    /* Form Box Container */
    .form-box {
      background: linear-gradient(135deg, #751f8a, #c24ecd); /* Gradient teal-cyan for vibrancy */
      border-radius: 20px;
      padding: 40px;
      width: 100%;
      max-width: 400px; /* Keep a maximum width */
      text-align: center;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
      color: #fff; /* White text inside the box */
    }

    /* Title */
    h1 {
      font-size: 2rem;
      margin-bottom: 25px;
      font-weight: 700;
      color: #ff9e2f; /* Bright orange for the title */
    }

    /* Form Label */
    .form-label {
      font-weight: 600;
      color: #f7f7f7; /* Light gray for the labels */
    }

    /* Form Fields */
    .form-control {
      background-color: #ffffff;
      color: #333;
      border-radius: 10px;
      padding: 15px;
      border: 1px solid #ddd;
      margin-bottom: 15px;
    }

    /* Focus Effect on Form Fields */
    .form-control:focus {
      border-color: #ff9e2f;
      box-shadow: 0 0 10px rgba(255, 158, 47, 0.5);
    }

    /* Submit Button */
    .btn {
      background: linear-gradient(135deg, #ff7b2f, #ff457d); /* Interactive gradient button */
      color: white;
      border-radius: 10px;
      padding: 15px;
      font-size: 1.2rem;
      width: 100%;
      transition: all 0.3s ease;
    }

    /* Button Hover Effect */
    .btn:hover {
      background: linear-gradient(135deg, #ff6a2c, #ff1f65); /* Darker gradient on hover */
    }

    /* Flash Message */
    .flash-message {
      background-color: #ff9e2f;
      color: white;
      padding: 15px;
      margin-bottom: 20px;
      border-radius: 10px;
      font-size: 1rem;
      text-align: center;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .container {
        flex-direction: column; /* Stack forms vertically for smaller screens */
        gap: 20px; /* Space between boxes on small screens */
      }

      .form-box {
        width: 100%; /* Make the form box take up full width */
        max-width: none;
        padding: 20px;
      }

      h1 {
        font-size: 1.5rem;
      }
    }
  </style>
</head>

<body>

  <div class="container">
    <!-- Sign-Up Form Box -->
    <div class="form-box">
      <h1>Sign Up</h1>
      <form action="/register" method="POST" class="needs-validation" novalidate>
        <div class="mb-3">
          <label for="username" class="form-label">Username</label>
          <input name="username" id="username" type="text" class="form-control" required />
        </div>
        <div class="mb-3">
          <label for="email" class="form-label">Email</label>
          <input name="email" id="email" type="email" class="form-control" required />
        </div>
        <div class="mb-3">
          <label for="password" class="form-label">Password</label>
          <input name="password" id="password" type="password" class="form-control" required />
        </div>
        <button class="btn">Sign Up</button>
      </form>
    </div>

    <!-- Login Form Box -->
    <div class="form-box">
      <h1>Login</h1>
      <form action="/login" method="POST" class="needs-validation" novalidate>
        <div class="mb-3">
          <label for="username" class="form-label">Username</label>
          <input name="username" id="username" type="text" class="form-control" required />
        </div>
        <div class="mb-3">
          <label for="password" class="form-label">Password</label>
          <input name="password" id="password" type="password" class="form-control" required />
        </div>
        <button class="btn">Login</button>
      </form>
    </div>
  </div>

  <script src="/javascript/script.js"></script>
</body>

</html>
